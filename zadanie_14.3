for x in range(1, 2031):

    f = 7 ** 170 + 7 ** 100 - x
    # Счётчик нулей в семеричной системе
    k = 0

    # Перебираем цифры в семеричной системе
    while f > 0:
        if f % 7 == 0:
            k += 1
        f = f // 7

    if k == 71:
        print(x)
